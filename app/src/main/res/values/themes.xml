<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Tema principal de la app -->
    <style name="Theme.ReparaFacil" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- colores base; si no tienes color.xml, puedes dejarlo así -->
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

</resources>
